# Archived Projects

このディレクトリは一時非公開プロジェクト置き場です。

- `projects/index.ts` の自動読込対象は `./published/*/index.ts` のみです。
- そのため、この `archive` 配下のファイルはビルド対象に含まれず、公開ページからは参照できません。

## 一時非公開にする方法

1. `published/<project-id>` ディレクトリを `archive/<project-id>` へ移動する
2. 必要に応じて公開時に `published` へ戻す

